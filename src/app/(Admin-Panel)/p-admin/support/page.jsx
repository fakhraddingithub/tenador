import { redirect } from 'next/navigation';

/**
 * صفحهٔ پشتیبانی — نقطهٔ ورودِ یکپارچه‌ی سایدبار.
 * فاز ۱: به‌طور موقت به «پیام‌های تماس» ری‌دایرکت می‌شود تا لینک ساید‌بار بشکسته نشود.
 * فاز ۲: این صفحه با سه تبِ «نظرات / پیام‌های تماس / اینستاگرام» جایگزین می‌شود
 * (کامپوننت‌های موجود بدون تغییرِ منطق داخل تب‌ها لود می‌گردند).
 */
export default function SupportRootPage() {
  redirect('/p-admin/support/messages');
}
