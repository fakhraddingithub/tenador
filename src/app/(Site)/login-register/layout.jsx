export const metadata = {
  title: "ورود یا ثبت‌نام",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function LoginRegisterLayout({ children }) {
  return children;
}
